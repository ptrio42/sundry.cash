# Changelog

## v2
- Fixed `sort-ascending`: the upward arrowhead no longer overlaps the top horizontal bar.
- Mirrored the spacing correction in `sort-descending` so the downward arrowhead cannot collide with the lower bars.
- The three bars now form a clearer progression and preserve a deliberate gap to the arrow.
